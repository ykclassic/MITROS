# Testing gates
Unit → Contract → Integration → Historical Replay → Backtest → Walk-forward/Purged Validation → Paper → Venue Sandbox/Demo → Production Shadow → Human-approved Production.
A phase cannot merge while required CI gates are red. Trading execution is never tested by silently submitting live orders.
